# Anthropic API key — required to generate courses
ANTHROPIC_API_KEY=sk-ant-...

# Vercel Postgres connection string — required at runtime
# Pull this from Vercel after creating the Postgres database:
#   vercel env pull .env.local
POSTGRES_URL=postgres://USER:PASSWORD@HOST/DATABASE?sslmode=require
